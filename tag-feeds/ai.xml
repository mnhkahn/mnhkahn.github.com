<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title>{{ site.title }} - AI</title>
  <link href="{{ site.url }}{{ page.url }}" rel="self"
        type="application/atom+xml" />
  <link href="{{ site.url }}" />
  <updated>{{ site.time |
    date_to_xmlschema }}</updated>
  <id>{{ site.url }}{{ page.url }}</id>
  <author>
        <name>{{ site.author.name | default: site.author }}</name>
        <email>{{ site.author.email }}</email>
    </author>
  <category term="Jekyll" />
    <!-- 仅筛选Jekyll标签的文章 --> {% for post in
    site.tags.AI %} <entry>
        <title>{{ post.title | xml_escape }}</title>
    <link href="{{ site.url }}{{ post.url }}" />
    <id>{{
    site.url }}{{ post.url }}</id>
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{
    post.updated | default: post.date | date_to_xmlschema }}</updated> {% for tag in post.tags %} <category
            term="{{ tag | xml_escape }}" /> {% endfor %} <content type="html">{{ post.content |
    xml_escape }}</content>
    </entry> {% endfor %} </feed>